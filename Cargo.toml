[workspace]
resolver = "2"
members = ["crates/kiem-core"]

# Kiem workspace. Tier 1 bootstraps `kiem-core` with the shared content-derivation
# rules (the single source of truth for note title + tags). Phases 1+ expand this
# crate (Automerge model, SQLite, search) and add `kiem-cli` / `kiem-ffi` members.
