[package]
name = "kiem-cli"
version = "0.1.0"
edition = "2021"
description = "Kiem CLI: human- and agent-facing command line interface over kiem-core."

[[bin]]
name = "kiem"
path = "src/main.rs"

[dependencies]
kiem-core = { path = "../kiem-core" }
