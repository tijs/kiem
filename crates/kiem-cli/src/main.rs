//! Kiem CLI placeholder. Real commands (create/list/show/edit/search/tags)
//! arrive in U4; this exists so the workspace shape is final from day one.

fn main() {
    eprintln!("kiem: CLI not implemented yet (see docs/plans, unit U4)");
    std::process::exit(1);
}
